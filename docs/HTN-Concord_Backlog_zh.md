# HTN-Concord — Epic 与工单（backlog）

> 🌐 **本文是 `HTN-Concord_Backlog.md` 的中文对照版。两份文件必须同步修改。**
> 若两边冲突，以**英文版为准**，并立刻回来修正本文。

`HTN-Concord_Master_Plan_zh.md` 的可执行拆解：8 个 epic（= 阶段）→ 工单，附验收标准、依赖、规模（S/M/L）。
状态：✅ 已完成 · 🟡 进行中 · ⬜ 待办 · 🔒 被依赖阻塞。工单编号（`HC-n`）稳定不变。

> ⚠️ **本文件不是事实来源（2026-07-18 更正）。** 实时追踪已经在 **Linear（GAI 看板，权威）**，并镜像到 **Notion 进度追踪库**。这份 markdown 是第三个追踪源，而它早已同时偏离了前两者 —— 它曾写「23 个测试」，Linear 写「28」，随后这条警告本身写了「113」，而套件实际已到 123。**这就是反对把数字写死的全部论据：连那句警告数字过期的话本身都过期了。** 请跑 `pytest -q`。把本文件当快照；每次变更都去更新 Linear + Notion，需要时**重新生成或删除**本文件，而不是手工修修补补。
>
> **追踪源对账（2026-07-18）。** 三个追踪源现已装载同一套工单。此前的状况是：HC-9/27/44/45 在 Notion 有而 Linear 没有；HC-23/29/43/46/47/48 只存在于本文件。**HC-8b 不是独立工单** —— derive.py 的两个 bug 都归在 HC-8 下。**HC-4a/HC-4b 是 HC-4 的子部分**，不是独立编号。

**关键路径（2026-07-18 修订）：** ~~HC-7（git ✅）~~ → HC-8（CKD 修复 ✅）→ HC-42（DecisionBuilder 重构，在 HC-34 *之前*）→ HC-34..39（引擎）+ HC-15..19（MIMIC，并行）→ HC-5/HC-39（联合设计）→ HC-50 → HC-53 → HC-60 → HC-70 → **HC-80（pilot/kill 门）**。

---

## EPIC E1 — 基础与规范契约 🟡

| ID | 工单 | 验收标准 | 依赖 | 规模 | 状态 |
|---|---|---|---|---|---|
| HC-1 | `patient_profile.schema.json` 契约 | schema 存在；覆盖数据字典 §6 全部字段；分期/性别/context 用枚举 | — | S | ✅ |
| HC-2 | 管线内 schema 校验 | 每一行写出前都校验；失败要报错而非静默 | HC-1 | S | ✅ |
| HC-3 | **共享控制词表模块** | 一个可导入模块承载药物类 / 合并症 / 禁忌 / ICD 锚点集；NHANES pipeline 与引擎都导入它（在 MIMIC 接入前就掐死漂移） | — | M | ✅ |
| HC-4 | 测试 harness + CI | 本地 `pytest` 全绿（跑 `pytest -q` —— **不要在此写死数字**）。**2026-07-18 已写好 workflow**（`.github/workflows/ci.yml`，提交 `e7081d1`）：push/PR 触发 pytest，锁定 Python 3.12，含「收集完整性」闸门 + `--strict-markers --strict-config` + 把已收集测试数发布到运行摘要。**CI 必须让 pytest *收集* 错误直接判定构建失败** —— 已实测：一个引用了不存在模块的测试，会让 123 个可收集测试**一个都跑不了**（`pytest -q` 退出码 2；而 `--continue-on-collection-errors` 会把它降级成退出码 1，workflow 里已明确警告绝不要加这个参数）。✅ **2026-07-18 已上线并实测全绿**（`github.com/AriaCong/htn-concord`，**私有仓库**）：连续两次运行成功，首次 = `29642244912`，8 个步骤全部成功，123 收集 / 123 通过（Python 3.12.13），与本地一致。仓库为私有时，徽章仅对协作者可见。 | — | S | ✅ |
| HC-5 | `llm_output.schema.json`（ModelRecommendation） | 结构化字段支持确定性推理链评分；能校验一份手写样例。✅ **2026-07-19 完成**：schema + 入库的手写样例（即 Master Plan 的 Task-B vignette）+ 防漂移测试 —— 把 `decision` 枚举钉死到 `engine.types.Decision`、药物类/禁忌枚举钉死到 `vocab.py`、抽取字段名钉死到 `patient_profile.schema.json`、推理链步骤的形状钉死到引擎的 `TraceStep`。「与 HC-39 联合设计」的约束正是这样满足的：链的形状*直接取自引擎*，HC-39 的发射器从此被测试契约约束 | **HC-39**（联合设计） | ~~S~~ **M** | ✅ |
| HC-6 | 仓库卫生 | 锁定依赖（✅）、lockfile、`pyproject`、原始输入 SHA256 manifest | — | S | 🟡 |
| HC-7 | **`git init` + `.gitignore` + 打标签的基线** | 项目纳入版本控制；gitignore 掉 `Data/` 与需授权的数据源 | — | S | ✅（`d94b5d9`，tag `baseline-2026-07-18`） |
| HC-9 | 运行 manifest 发射器 | 每次 pipeline 运行输出 git SHA + 输入/输出 SHA256 + 解析后的配置 + 行数 | HC-7 | S | ⬜ |
| HC-29 | 事实/标签拆成独立产物 | 面向模型的事实与隐藏标签物理分离落盘 | HC-1 | M | ⬜ |
| HC-56 | **测试夹具里混入了真实 MIMIC 记录** | 夹具中不得含任何逐字照抄的受授权数据；结论写进 README。**卡住 HC-90** | — | S | ⬜ |

## EPIC E2 — 数据清洗：所有数据源 → PatientProfile 🟡

| ID | 工单 | 验收标准 | 依赖 | 规模 | 状态 |
|---|---|---|---|---|---|
| HC-10 | NHANES pipeline | 4,806 份档案，schema 合法，QA 报告（✅ 已建 + 已应用评审修复） | HC-1 | L | ✅ |
| HC-11 | NHANES NCHS 关联死亡率解析器（Task D） | 解析定宽 `.dat`；按 SEQN 连接 MORTSTAT/PERMTH | HC-10 | M | ⬜ |
| HC-12 | NHANES `P_` 合并周期运行 | `NHANES_CYCLE=P_pre_pandemic` 产出合法档案，带 WTMECPRP | HC-10 | S | ⬜ |
| HC-13 | **MIMIC 可行性/衰减门（只计数）** | 产出量查询 + 锚点→≥2 次既往 OMR 的衰减瀑布图；决定 v1 还是 730 天/≥1 的回退 | HC-3 | M | ✅（血压规则可行：28,530 —— **但真正可*决策*的队列约为 8,921（31.3%）**，即有 `ed/medrecon` 的那个子集，而它是唯一不泄漏的 `on_bp_meds` 来源；见 HC-26） |
| HC-14 | MIMIC OMR 血压解析器 + context flag | 解析 `"SBP/DBP"`；处理体位；每行都带慢性/门诊/入院 flag | HC-13 | M | ✅（`pipelines/mimic/omr_bp.py`，30 个测试） |
| HC-15 | MIMIC labevents 加载器 + eGFR | 按 itemid 过滤加载（肌酐=50912，钾=50971）；用 `valuenum`；取离 index 最近值；派生 eGFR | HC-13、**HC-27** | M | ⬜ |
| HC-16 | MIMIC ICD-9/10 合并症+禁忌交叉映射 | 一份交叉映射 → 糖尿病/CKD/血管神经性水肿/妊娠 flag；时间限定在 admittime 之前 | HC-3 | M | ⬜ |
| HC-17 | MIMIC index 前用药重整 | 家庭用药取自 `ed/medrecon`（绝不用出院带药）；`etcdescription`→药物类 | HC-3、HC-20、**HC-26** | M | ⬜ |
| HC-18 | MIMIC 队列构建器（3 个子队列） | 产出 Primary / OMR-only / Text-robustness；最早锚点；schema 合法 | HC-14..17 | L | ⬜ |
| HC-19 | MIMIC Task-C 病历 + 银标准 | 出院小结选择；银标准来自 dx+labs+medrecon；人工验证一个样本 | HC-18 | M | ⬜ |
| HC-20 | MIMIC-ED pipeline | medrecon 药物类集合；分诊/生命体征标为急性 context；edstays 连接 | HC-1 | M | ⬜ |
| HC-21 | eICU pipeline —— **降级** | ~~完整 pipeline~~ → 仅约 **500 例住院的弃权校准探针**（模型会不会正确拒绝对 ICU 血压做慢性分级？）。eICU 每条血压都是急性，完整构建会产出约 20 万行没有决策标签的数据。注：eICU *确实*带 `note.csv`（306 MB），但那是路径/数值片段而非叙事文本，因此仍然没有 Task C | HC-3 | S | ⬜ |
| HC-22 | Zigong HF 轻清洗 —— **建议砍掉** | 2,008 名中国心衰住院病人；没有降压决策语境、没有病历文本、也不是原发性高血压。「中国指南分歧」检查由 ESC-2024 对照模块（HC-41）在真实队列上做要好得多 | — | S | ⬜ |
| HC-23 | **落实已决定的中位数血压统一** | `clean.py` 改用中位数、重跑 pipeline、把全部下游数字重新引用一遍 | HC-10 | S（代码）/ 基准冻结级（影响） | ⬜ |
| HC-24 | 接上 NHANES 妊娠 flag（`RIDEXPRG`）—— **安全** | 读 `DEMO_J.RIDEXPRG`，置妊娠禁忌 flag，让引擎弃权 | HC-10 | S | ⬜ **P0** |
| HC-25 | 下载 `MCQ_J`，填充 `clinical_cvd` | `MCQ160B–F`（心衰/冠心病/心绞痛/心梗/卒中）→ 经 Kleene 或运算得 `clinical_cvd` | HC-10 | S | ⬜ |
| HC-26 | MIMIC ED 关联队列决策 | 在已知真实决策队列 N 的前提下确定 index 就诊规则 | HC-13 | M | ⬜ |
| HC-27 | 把 MIMIC 化验严格限定在 `admittime` 之前 | 取严格早于 admittime 的最近一次化验，且落在预注册的回溯窗内 | HC-13 | S | ⬜ |
| HC-8 | 修复 `derive.py` 正确性缺陷 | ✅ **已完成** —— 详见下方审计表 | HC-10 | S | ✅ |

## EPIC E3 — 确定性指南引擎（Aria 手写规则）🟡

| ID | 工单 | 验收标准 | 依赖 | 规模 | 状态 |
|---|---|---|---|---|---|
| HC-30 | **PREVENT 脚手架** | 纯模块：输入组装（含 statin）、30–79 岁门、单位换算、写明函数形式、系数占位、测试；系数填入前返回 None | HC-10 | M | ✅ |
| HC-31 | **PREVENT 系数 + 验证** | 转录 Khan 2024 分性别系数；复现一个已发表校验样例的风险值；启用 | HC-30 | M | ✅（精确复现表 S12A） |
| HC-32 | 血压分期规则模块 + 测试 | 带引用的引擎规则包装分期逻辑；边界测试 | HC-3 | S | ✅（`engine/rules/aha_acc_2025/staging.py`） |
| HC-33 | 启动/强化规则 | 消费 PREVENT ≥7.5% 门 + 三值弃权；Stage-1 风险驱动（年龄经由 PREVENT，而非独立变量）；NaN/numpy 安全 | HC-31 | M | ✅（2026-07-18 **从零**建成；此前「已存在（9 个测试）」的说法不属实） |
| HC-42 | **`DecisionBuilder` 重构 —— 在 HC-34 之前** | 可变 builder 累积引用/推理链，`evaluate()` 冻结一次；规则变成「贡献者」 | HC-33 | M | ⬜ |
| HC-43 | `evaluate()` 中的 `guideline` 字段 + 注册表分发 | 决策类型带 guideline 字段；经注册表分发到各指南模块 | HC-42 | S | ⬜ |
| HC-34 | 一线药物类选择（种族中立） | 噻嗪/ACEI/ARB/二氢吡啶类 CCB；优选双药单片复方；有测试 | HC-42 | M | ⬜ |
| HC-35 | 合并症修饰项 | **CKD = eGFR <60 *或* UACR ≥30 mg/g**（是「或」不是「且」—— KDIGO 与指南原文措辞）→ 优选 ACEI/ARB；有测试、有引用。字段改名 `ckd_albuminuria` → `ckd`。禁忌优先级高于本偏好。MIMIC 需要 `labevents` itemid 51070 取 UACR | HC-34 | S | ⬜ |
| HC-36 | 禁忌症规则 | 妊娠/血管神经性水肿/K⁺≥5.5 的规避逻辑；有测试、有引用。⚠️ 实现前务必读 Master Plan 阶段 3 的完整规范：**低 eGFR 不是禁忌** | HC-3 | M | ⬜ |
| HC-37 | 弃权逻辑 | 输入为 NA 或急性 context 血压 → ABSTAIN/not_encoded；有测试 | HC-32、HC-36 | S | ⬜ |
| HC-28 | **运行时范围外拦截** | 对难治性/继发性高血压、终末期肾病、儿科、妊娠、高血压急症弃权；新增重度（>180/120）分级 | HC-37 | M | ⬜ |
| HC-44 | 取得指南 PDF；把锚点映射到印刷章节 | 每个符号化引用锚点都绑定到 2025 指南的已核实印刷章节 | — | M | ⬜ |
| HC-38 | 引用链接器 | 每个决策 → 指南锚点 id | **HC-44**（不是 HC-33..37 —— 必须先把锚点映射到印刷章节；旧依赖写错了） | S | ⬜ |
| HC-39 | 推理链发射器 | 为 `trace_concordance` 产出标准推理链 | HC-33..37 | M | ⬜ |
| HC-45 | 引擎集成运行 + 弃权报告 | 对全部阶段 2 档案跑引擎；发布弃权率与弃权原因直方图 | HC-34..39 | S | ⬜ |
| HC-40 | MedQA/USMLE 高血压 sanity 集 | 精选的高血压题目全部通过 | HC-33..37 | S | ⬜ |
| HC-41 | ESC 2024 模块 + 分歧报告 | 独立的 `evaluate()`；量化 AHA-vs-ESC 分歧 | HC-33..38、HC-43 | L | ⬜ |

## EPIC E4 — 任务构建（Aria 手写渲染器）⬜

| ID | 工单 | 验收标准 | 依赖 | 规模 | 状态 |
|---|---|---|---|---|---|
| HC-50 | 病例渲染器（3 档难度） | `render(profile, level, seed)`；只含事实；确定性。✅ **2026-07-19 完成**：`renderer/` —— 三档难度，确定性（用 SHA-256 播种，而非按进程加盐的 `hash()`），每次调用都会用 `leakage.scan` 自检。**设计不变量：难度只改变"呈现方式"，绝不改变"信息量"** —— 三档携带完全相同的决策相关事实，因此按难度下降的准确率只能归因于"抽取变难"，而不是"病例根本没说"。绝不渲染 `bp_stage`/`prevent_10yr`/`ckd_albuminuria`/禁忌标志，但会渲染它们的全部原始输入，所以没有任何一题变得无法作答。**血压取下整（floor）而非四舍五入**：NHANES 血压是均值（131.667），四舍五入会跨过分期阈值 —— 那会让 **4,806 份档案中的 93 份**显示出比自身隐藏标签高一个分期的血压 | HC-18/HC-10 | L | ✅ |
| HC-51 | 干扰项库 | 手写、经一次性验证；家族史 vs 病人事实的陷阱。**尚未开始 —— 这正是限制 `hard` 档的缺口**：HC-50 的 `hard` 填充内容在临床上是惰性的（就诊流程细节），所以目前 `hard` 只是*更难抽取*，而不是*更难推理*。通过 `distractors=` 钩子接入渲染器。按设计应由临床医生（Aria）撰写 | HC-50 | M | ⬜ |
| HC-52 | Task A 构建器 | 干净档案 → 引擎标签 | HC-33..38 | S | ⬜ |
| HC-53 | Task B 构建器 + 泄漏审计 | 渲染病例 → 隐藏标签；文本中无标签 token。✅ **2026-07-19 完成**：`tasks/` —— 输入与标签写入**物理隔离的两个文件**，仅通过 `case_id` 关联（原则 4）；审计是**构建闸门而非报告**（不合格的语料根本不会被写出）；划分按**病人**而非按 case（同一病人的三档难度不可能横跨 train/test），且**可随语料增长保持稳定**（哈希阈值法，因此新增周期/MIMIC 不会移动已有病人）。冻结语料：4,806 位病人 → **14,418 个 case，审计通过，0 个违禁词命中、0 个标签列命中**；校验和见 `benchmarks/task_b_manifest.json`，可用 `tasks.verify()` 复核。⚠️ **人工那一半仍未完成** —— 需 Aria 人工通读 20 份病例确认"只含事实"；核对表由 `tasks/spotcheck.py` 生成。当前仅基于 HC-50 构建，尚未纳入 HC-51 干扰项 | HC-50、HC-51、HC-46 | M | ✅（机器部分）；⬜（人工抽检） |
| HC-46 | 泄漏审计 lint（禁用词扫描器） | 任何面向模型的文本若含类别名/决策动词/「……的禁忌症」即判失败。**扫描器已存在**：`htn-concord/leakage.py`（2026-07-19），由渲染器的逐次自检与 HC-53 的构建闸门共享。匹配以词边界为锚，这一点是关键：若按子串匹配，会把每一份**氢氯噻嗪**（本队列中最常见的降压药）病人的病例全部误判为泄漏。剩余范围仅剩包装 `leakage.scan` 的**独立 CLI lint** | HC-29 | S | 🟡 |
| HC-54 | Task C 构建器 | 病历 → 抽取 → 银标准评分 → 引擎误差传播 | HC-19 | M | ⬜ |
| HC-55 | Task D 关联 | 一致性 ↔ NHANES 死亡率 / MIMIC dod | HC-11 | S | ⬜ |

## EPIC E5 — 模型 harness ⬜

| ID | 工单 | 验收标准 | 依赖 | 规模 | 状态 |
|---|---|---|---|---|---|
| HC-60 | JSON 模式 runner | 输出符合 `llm_output.schema.json`；可确定性重放。✅ **2026-07-19 完成**：`runner/` —— `run_case()` + provider 接缝（scripted/replay/Anthropic）、对完整 HC-5 schema 的本地严格校验、畸形 JSON 重试、带 prompt/schema 哈希 + token 用量 + 成本 + 延迟的 transcript，以及 `replay()`。⚠️ `AnthropicProvider` **只写了、没真跑过**（环境里没有 SDK 也没有凭证）—— 见 `runner/README.md`；在真正跑一次冒烟之前，畸形 JSON 率**尚未测量** | HC-5 | M | ✅ |
| HC-61 | 模型适配器 | ≥1 前沿 + ≥1 开源权重 | HC-60 | S | ⬜ |
| HC-62 | 三种条件 | vanilla / 朴素 RAG / 指南图 | HC-60 | M | ⬜ |
| HC-63 | 可复现基础设施 | ~~seed~~、日志、缓存、成本/延迟记录。**根本不存在 seed** —— 见 HC-64；成本/延迟已由 HC-60 在每次调用时记录 | HC-60 | S | ⬜ |
| HC-64 | **「temperature/seed 锁定」根本无法实现** | 当前前沿模型会以 400 拒绝 `temperature`/`top_p`/`top_k`，且从来就没有 `seed`。可复现性只能落在 transcript 重放上，而不是采样控制上。需修正阶段 5 的措辞（✅ 已完成）、决定基准冻结时是否锁死 `effort`，并在方法部分明确写清是**存档级**还是**采样级**可复现 | HC-60 | S | ⬜ |
| HC-47 | 响应缓存 + transcript 存储 | 缓存模型响应；持久化每一次请求/响应 | HC-60 | M | ⬜ |
| HC-48 | Prompt 模板版本化 + 哈希 | 每个模板带版本；每次运行记录其哈希 | HC-60 | S | ⬜ |

## EPIC E6 — 评估与失败模式审计 ⬜

| ID | 工单 | 验收标准 | 依赖 | 规模 | 状态 |
|---|---|---|---|---|---|
| HC-70 | 指标 | 计划中全部 9 项指标；能复现人工评分的 gold set | HC-52..54 | M | ⬜ |
| HC-71 | 失败模式分类器 | 每个失误标注为 抽取/推理/引用 | HC-70 | M | ⬜ |
| HC-72 | 亚组 + 难度报告 | 按 性别/年龄/合并症 × 难度档 | HC-70 | S | ⬜ |

## EPIC E7 — 实验 ⬜

| ID | 工单 | 验收标准 | 依赖 | 规模 | 状态 |
|---|---|---|---|---|---|
| HC-80 | **Pilot 尖峰 + kill 标准** | 20 病例 × 3 难度；2 个模型；按失败模式的表格；go/no-go | HC-53、HC-60、HC-70 | M | ⬜ |
| HC-81 | RQ1/RQ2/RQ3 完整运行 | 可复现性、失败定位、图 vs 基线 | HC-80 | L | ⬜ |
| HC-82 | Task D 合理性 | 一致性↔死亡率关联（绝不因果） | HC-55、HC-70 | S | ⬜ |

## EPIC E8 — 写作与发布 ⬜

| ID | 工单 | 验收标准 | 依赖 | 规模 | 状态 |
|---|---|---|---|---|---|
| HC-49 | **临床医生表面效度 + 150–200 例裁定** | 专科医生规则级审查 + 2 名医生盲法独立裁定；报告 Cohen's κ，并**预先声明 κ < 1.0 属预期** | HC-34..39 | M | ⬜ **Paper 1 的 P0** |
| HC-90 | 开源引擎 + 基准 | 可复现发布，含 manifest、校验和、QA | HC-81 | M | ⬜ |
| HC-91 | 论文 | 1 篇旗舰 vs 基准+方法拆两篇（Aria 定） | HC-81 | L | ⬜ |

---

### 即刻冲刺（2026-07-18 重写 —— 此前那份列表里全是已完成的工单）

1. ~~**HC-7** `git init` + `.gitignore` + 打标签的基线~~ —— ✅ **2026-07-18 完成。** 基线 `d94b5d9`，tag `baseline-2026-07-18`，分支 `main`，已推送至私有仓库 `AriaCong/htn-concord`。已解锁 CI（HC-4 ✅）、运行 manifest（HC-9）与发布（HC-90）。
2. ~~**HC-4** CI workflow + 测试徽章~~ —— ✅ **2026-07-18 完成。** 已在 `AriaCong/htn-concord`（私有）上线并全绿，连续两次运行成功，123/123（Python 3.12.13）。
3. **HC-9** 运行 manifest（git SHA + 输入/输出 SHA256 + 解析后配置），两条 pipeline 都要。
4. **HC-42** `DecisionBuilder` 重构 —— 必须在 **HC-34 之前**，否则「先累积再冻结」的模式会在六个规则模块里各复制一遍。与 **HC-43** 配对做。
5. **HC-5 + HC-39 联合设计** —— 它们是引擎与 LLM 那一半之间的契约；先写 HC-5 等于猜推理链的形状，事后还得重写。*（2026-07-19 已解决：HC-5 ✅ 没有自创链形状，而是直接采用引擎已有的 `engine.types.TraceStep`，并用防漂移测试断言 schema 的 TraceStep 与该 dataclass 逐字段相等 —— HC-39 的发射器继承的是一份已固定的契约，而不是让 schema 去猜一个未来的格式。）*
6. **HC-24 / HC-25 / HC-28** —— 安全与标签有效性，必须赶在 HC-36 落地之前。

### 2026-07-18 三评审员审计新开的工单

| ID | 工单 | 缘由 | 优先级 |
|---|---|---|---|
| HC-7 | `git init` + 基线提交 | ✅ **2026-07-18 完成** —— 提交 `d94b5d9`，分支 `main`，tag `baseline-2026-07-18`；51 个文件 / 408 KB；`Data/`（约 60 GB 需授权数据）与受版权保护的 PREVENT 论文均已排除，并在提交前核实未被暂存。**基线是「修复之后」的状态**：HC-8 的修复早于仓库存在，因此没有「修复前」的参照点可提交，也**没有**去伪造一个 | P0 |
| HC-8 | 修复 `derive.py` 正确性缺陷 —— **两个都算在这一单里**：`ckd_albuminuria` 的 NA 强转（未知→False，击穿弃权契约；未知 0 → 266）**以及** `bp_stage` 的 `between()` 边界 bug（把 4,806 行中 56 行合法的非整数血压判空；档案列与引擎静默矛盾） | ✅ **已完成** —— 缺失 56 → 0，档案重新导出并含 `clinical_cvd`（30 → 31 列），并新增边界网格测试，保证 `derive` 与 `vocab` 的分级永不分叉。*（不存在独立的「HC-8b」工单 —— 两个 bug 在 Linear 与 Notion 里都归在 HC-8 下。）* | P0 |
| HC-9 | 运行 manifest 发射器 | 产物已经与生成它的代码发生漂移 | P0 |
| HC-23 | 落实已决定的**中位数**血压统一 | 所有文档都说 2026-07-18 已统一为中位数，而 `clean.py` 仍用 `mean()` —— 于是交付的底料是均值产物，所有被引用的分期/标签数字描述的都是均值管线。属基准冻结级操作：改代码、重跑、重新引用，一次做完 | P1 |
| HC-24 | 接上 `DEMO_J.RIDEXPRG` 妊娠 flag | 45 名孕妇禁忌列表为空 → HC-36 一落地就会产出不安全的标准答案 | P0 |
| HC-25 | 下载 `MCQ_J`，填充 `clinical_cvd` | 该触发因子目前对每一行 NHANES 都是 null → 偏向治疗不足 | P1 |
| HC-26 | MIMIC ED 关联队列决策 | 真实决策队列 N 约 8,921，而非 28,530 | P1 |
| HC-27 | 把 MIMIC 化验严格限定在 `admittime` 之前 | 「取离 index 最近的值」是双向的 → index 后泄漏 | P1 |
| HC-28 | 运行时范围外拦截 | 引擎对难治性/继发性高血压、妊娠、终末期肾病、高血压急症都会自信作答 —— 全都声明在范围外却无人拦截 | P1 |
| HC-29 | 事实/标签拆成独立产物 | 防泄漏原则 #4 只被声明、未在数据层执行 | P1 |
| HC-42 | `DecisionBuilder` 重构 | HC-34 之前需要一个组合缝 | P1 |
| HC-43 | `evaluate()` 的 `guideline` 字段 + 注册表分发 | 现在免费，HC-41 之后就是侵入式改动 | P1 |
| HC-44 | 取得指南 PDF；把锚点映射到印刷章节 | HC-38 与 `citation_support` 指标的硬前置 | P1 |
| HC-45 | 引擎集成运行 + 弃权报告 | 阶段 3 的验收门此前没有工单；跑一次就能立刻抓到 HC-8 | P1 |
| HC-46 | 泄漏审计 lint（禁用词扫描器） | ~~在 HC-53 的验收标准里被引用，却从来不是一个交付物~~ —— **扫描器现已存在**（`htn-concord/leakage.py`，2026-07-19）；剩余范围仅剩独立 CLI lint | P2 |
| HC-47 | 响应缓存 + transcript 存储 | 让分析在推理不确定的前提下仍可复现 | P2 |
| HC-48 | Prompt 模板版本化 + 哈希 | 未版本化的 prompt 改动会静默让此前所有运行作废 | P2 |
| HC-56 | 测试夹具里混入了真实 MIMIC 记录 | `tests/test_mimic_omr_bp.py` 内含 `10000032,2180-04-27,1,Blood Pressure,110/65`，该行**逐字**存在于 `omr.csv.gz`。PhysioNet 的 DUA 禁止再分发 —— 仓库私有时风险可控，但**卡住 HC-90**。要么确认它属于 ODbL 授权的 demo 子集并记录结论，要么换成合成数值 | P1 |
| HC-49 | 临床医生表面效度 + 150–200 例裁定 | 「无临床医生参与」按目前写法在评审上过不去 | **Paper 1 的 P0** |
